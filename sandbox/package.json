{
  "dependencies": {
    "@linear/sdk": "^10.0.0",
    "dotenv": "^16.3.1"
  },
  "name": "linear-sandbox",
  "version": "1.0.0",
  "description": "Experiments with the linear api",
  "main": "linear.js",
  "scripts": {
    "tsc": "tsc",
    "compile": "tsc && node linear.js",
    "dev": "nodemon --watch . --watch .. --watch ../src -e ts --exec \"npm run compile || exit 1\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nicholas Beninato",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "type": "module"
}
